interface:
  display_name: "MCP 前端逆向作业规范"
  short_description: "前端 JS 逆向：补环境、插桩、去混淆"
  default_prompt: "使用 $jsreverser-mcp-playbook 对目标站做签名链路逆向，按 Hook 优先流程完成补环境、插桩与证据化输出。"

policy:
  allow_implicit_invocation: true
