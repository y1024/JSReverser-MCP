# run/

这里放 **可执行 task-local 脚本**，不要把这些脚本搬去公开 case。

## 起手约定

- 当前推荐入口：`run/run-local.mjs`
- 单次校验入口：`run/verify-once.mjs`
- 如果开始出现多个执行脚本，再按职责拆 `core/`、`verify/`、`trace/`、`legacy/`

## 使用建议

请始终在这里写清：

1. 当前主入口文件
2. 最常用 1~3 条命令
3. 哪些脚本只是实验性脚本
4. 哪些脚本已经归档，不再继续叠新逻辑
